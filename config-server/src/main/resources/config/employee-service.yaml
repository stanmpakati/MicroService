server:
  port: 8002

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    registerWithEureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0
